import { createBrowserHistory } from 'history';

// this function create an instance of hisory and export as default 
// we will use it in Router to control push history

export default createBrowserHistory();

